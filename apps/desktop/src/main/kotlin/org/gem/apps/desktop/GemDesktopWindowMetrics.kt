package org.gem.apps.desktop

import androidx.compose.ui.unit.Dp
import org.gem.ui.design.GemSpacing

object GemDesktopWindowMetrics {
    val initialWidth: Dp = GemSpacing().desktopWindowWidth
    val initialHeight: Dp = GemSpacing().desktopWindowHeight
}
